directive @auth(role: Role = USER) on FIELD_DEFINITION
scalar Time

enum Role {
  USER
  ADMIN
}

enum SortDirection {
  ASC
  DESC
}

scalar DateTime

type Response {
  success: Boolean!
  message: String
}
